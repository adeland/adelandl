// Skills, expressed as a poker range. The About section renders these as a
// 13×13 GTO range chart: `raise` = expert (the chart's premium region),
// `call` = proficient (the surrounding band). Everything else reads FOLD.
// List as many as you like — cells cycle through them on hover.
export const skillRange = {
  raise: ['[Core Skill One]', '[Core Skill Two]', '[Core Skill Three]'],
  call: ['[Skill Four]', '[Skill Five]', '[Skill Six]', '[Skill Seven]'],
};
